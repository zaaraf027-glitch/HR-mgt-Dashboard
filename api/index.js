// This file exists solely to satisfy Vercel's zero-config requirements.
// By placing this in the /api folder, Vercel automatically detects it as a Serverless Function
// without needing the legacy "builds" configuration in vercel.json.

module.exports = require('../server.js');
